import { ComputersCanvas } from './canvas';

const Hero = () => {
  return (
    <section className={`relative w-full h-screen mx-auto`}>
      <ComputersCanvas />
    </section>
  );
};

export default Hero;
